# Basis-URL des Backends.
#
# Leer lassen oder weglassen für die lokale Entwicklung: dann wird /api verwendet und der
# Vite-Dev-Server leitet auf http://localhost:8080 weiter (siehe vite.config.ts).
#
# Für einen Deploy, bei dem das Frontend unter einer anderen Herkunft läuft als das Backend,
# hier die vollständige URL setzen. Achtung: das Backend hat derzeit keine CORS-Konfiguration,
# ein solcher Betrieb braucht also entweder CORS im Backend oder einen gemeinsamen Reverse Proxy.
# VITE_API_BASE_URL=https://beispiel.internal/portfolio-backend
